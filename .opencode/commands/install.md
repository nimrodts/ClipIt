---
description: Build and install the debug APK
agent: build
---

Run `./gradlew installDebug` to build and install the debug APK on the connected device/emulator. Report whether the installation succeeded or failed.
